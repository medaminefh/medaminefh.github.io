/** @type {import('tailwindcss').Config} */
// eslint-disable-next-line no-undef
module.exports = {
  darkMode: "class",
  theme: {
    extend: {
      animation: {
        "spin-slow": "spin 25s linear infinite",
      },
    },
  },

  plugins: [],
};
